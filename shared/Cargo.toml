[package]
name = "dopablocker-shared"
version = "0.1.0"
edition = "2021"
description = "Shared models, bloom filter, and domain matching for DopaBlocker"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
