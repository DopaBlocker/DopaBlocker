// Inicialização e helpers do Firebase Auth.
// Implementar: initFirebase(), signInWithEmail(email, password),
// signInWithGoogle(), createAccount(email, password), signOut(),
// getCurrentUser(), getIdToken() para obter JWT para requests ao backend,
// onAuthStateChanged stream.
