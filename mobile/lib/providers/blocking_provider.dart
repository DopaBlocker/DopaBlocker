// Riverpod provider para regras de bloqueio.
// Implementar: StateNotifierProvider com BlockingState (blocklist, isBlockingActive,
// isAdultFilterEnabled, blockMode). Métodos: fetchBlocklist(), addItem(),
// removeItem(), toggleBlocking(), toggleAdultFilter().
// Comunicar com Kotlin nativo via blocking_channel para ativar/desativar VPN.
