// Riverpod provider para gestão de dispositivos.
// Implementar: StateNotifierProvider com DeviceState (devices, linkedDevices, linkCode).
// Métodos: registerDevice(), generateLinkCode(), confirmLinkCode(code),
// getLinkedDevices(). Sincronizar com backend via api_client.
