// Riverpod provider para estado de autenticação.
// Implementar: StateNotifierProvider com AuthState (user, isAuthenticated, isLoading).
// Métodos: login(email, password), loginWithGoogle(), logout(), checkAuthState().
// Integrar com firebase_service.dart e sincronizar com backend.
