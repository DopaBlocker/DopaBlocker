// Widget tile para exibir item bloqueado na lista.
// Implementar: ListTile com ícone (site/app), nome do item,
// status (ativo/inativo), botão de remover (swipe ou ícone).
// Recebe BlockedItem como parâmetro.
