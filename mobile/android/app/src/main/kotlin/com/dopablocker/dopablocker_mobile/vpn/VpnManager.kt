// Gerenciamento do ciclo de vida da VPN (prepare, start, stop).
// Implementar: prepare() para solicitar permissão VPN ao usuário,
// start() para iniciar DnsVpnService, stop() para parar o serviço,
// isActive() para verificar se a VPN está rodando.
// Gerenciar intent para VpnService.prepare() e startForegroundService().

package com.dopablocker.dopablocker_mobile.vpn
