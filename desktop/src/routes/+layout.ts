// SvelteKit rodando dentro do webview do Tauri — sem servidor, sem SSR, sem
// prerender. A saída é um único index.html servido pelo Tauri como app SPA.

export const ssr = false;
export const prerender = false;
