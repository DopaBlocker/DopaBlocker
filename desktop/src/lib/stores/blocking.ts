// Svelte store para estado das regras de bloqueio.
// Implementar: writable store com blocklist (BlockedItem[]), isBlockingActive,
// isAdultFilterEnabled, blockMode (Personal/Parental).
// Funções: fetchBlocklist(), addItem(), removeItem(), toggleBlocking(), toggleAdultFilter().
// Sincronizar com Tauri backend via tauri-bridge.
