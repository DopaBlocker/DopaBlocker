// Svelte store para estado de autenticação.
// Implementar: writable store com user (User | null), isAuthenticated, isLoading.
// Funções: login(email, password), loginWithGoogle(), logout(), checkAuthState().
// Integrar com Firebase Auth SDK e sincronizar com o backend.
