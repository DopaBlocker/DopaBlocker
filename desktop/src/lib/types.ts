// Tipos TypeScript compartilhados pelo frontend desktop.
// Implementar: interfaces User, Device, BlockedItem, ParentalLink,
// enum BlockMode (Personal, Parental), type BlockItemType (site, app),
// interfaces de request/response para a API (LoginRequest, BlocklistResponse, etc.).
