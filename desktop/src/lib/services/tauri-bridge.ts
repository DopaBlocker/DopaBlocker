// Wrappers de invoke() do Tauri para comandos Rust.
// Implementar: funções tipadas que chamam os comandos registrados no Tauri backend:
// getBlocklist(), addBlockedItem(type, value), removeBlockedItem(id),
// toggleBlocking(enabled), toggleAdultFilter(enabled),
// generateLinkCode(), confirmLinkCode(code), getLinkedDevices().
// Usar @tauri-apps/api/core para invoke().
