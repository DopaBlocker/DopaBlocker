// Carregador de bloom filter para domínios adultos.
// Implementar: baixar/carregar listas open-source (Steven Black / OISD),
// popular o BloomFilter do crate shared com os domínios.
// Funções: load_adult_domains() -> BloomFilter, update_list() (atualiza da internet),
// is_adult_domain(domain) -> bool (consulta o bloom filter).
