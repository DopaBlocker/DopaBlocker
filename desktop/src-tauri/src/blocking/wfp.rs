// Integração com Windows Filtering Platform via FFI.
// Implementar: usar a WFP API (fwpuclnt.dll) para adicionar filtros de rede
// que bloqueiam conexões TCP/UDP para IPs resolvidos dos domínios bloqueados.
// Funções: add_filter(domain), remove_filter(domain), clear_all_filters().
// Requer execução com privilégios de administrador.
