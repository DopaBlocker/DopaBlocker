# Docker Compose para desenvolvimento local.
# Sobe o backend Rust e expõe na porta 3000.
# Volume persistente para o banco SQLCipher (criptografado).

services:
  backend:
    build: ../backend
    ports:
      - "3000:3000"
    env_file:
      - ../backend/.env
    environment:
      - SQLCIPHER_KEY=${SQLCIPHER_KEY}
    volumes:
      - backend-data:/app/data
    restart: unless-stopped

volumes:
  backend-data:
