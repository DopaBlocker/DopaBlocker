// Middleware de autenticação — validação de Firebase JWT.
// Implementar: extrair token Bearer do header Authorization,
// validar o JWT contra as chaves públicas do Firebase,
// injetar o user_id decodificado no request extensions para uso nos handlers.
