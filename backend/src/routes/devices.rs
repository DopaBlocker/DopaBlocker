// Registro de dispositivos e vinculação parental.
// Implementar: POST /devices/register (registra dispositivo na conta),
// POST /devices/link/generate (gera código 6 dígitos com TTL 5 min),
// POST /devices/link/confirm (confirma vinculação pai-filho via código),
// GET /devices (lista dispositivos vinculados à conta).
