// Endpoints de autenticação.
// Implementar: POST /auth/register (cria usuário no Firebase + DB local),
// POST /auth/login (valida credenciais, retorna token),
// GET /auth/me (retorna dados do usuário autenticado).
